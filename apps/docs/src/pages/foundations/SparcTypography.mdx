import {
  SparcFontPreview,
  SparcSizeScale,
  SparcWeights,
} from '../../components/foundations/SparcFoundation';

<PageHeader
  title="SPARC Typography (staging)"
  description="Typography tokens externally collected from the SPARC apps (carltonLaborPlanning globals.css, verified identical to commonforge-sparc-ui). Extracted 2026-07-21 — pending review, not yet merged into pod-test-tokens."
/>

<div className="my-6 rounded-lg border border-warning/40 bg-warning/15 p-4 text-sm text-default">
  <p className="font-semibold text-warning">⚠ Staging content — not official POD tokens</p>
  <p className="mt-1 text-subtle">
    Everything on this page was collected from external SPARC app code for review
    (status: <em>collected, ready for review; not yet merged into design-system</em>).
    Values are rendered straight from <code>src/data/sparc-foundation.json</code> via
    inline styles — no <code>pod-test-tokens</code> classes exist for them yet. Do not
    reference these tokens in production components.
  </p>
</div>

## Font family

SPARC uses **Geist** with Inter and system fallbacks.

<SparcFontPreview />

**No mono token is shipped by decision** (2026-07-21): neither Geist Mono nor
Space Mono. Mono usages (code snippets, job numbers, JSON logs) fall back to the
platform monospace stack via Tailwind's default `font-mono`, which needs no token.

## Size scale

Seven sizes in the collected scale (`text-3xs` → `text-body-lg`), of which three
— `text-xs`, `text-body`, `text-body-lg` — equal Tailwind stock values and are
marked as stock context rows. The collected line-heights **override** Tailwind's
stock `leading-tight` / `leading-snug`.

<SparcSizeScale />

## Weights

<SparcWeights />

These are Tailwind stock classes as used across the SPARC components
(`font-medium` 500 is the workhorse); they are documented here, not yet tokens.

## Root font-size — "THE ONE KNOB"

Root font-size is fixed at **16px**. All component sizing is authored in rem
against it, so the whole UI scales from that single value.

## Source of truth

Values on this page come from `apps/docs/src/data/sparc-foundation.json`, copied
unchanged from the external collection (`design-system-testing-abdillah/foundation/tokens.json`,
v0.3.1). If these tokens are adopted, they move into `packages/tokens` and this
page is replaced by the standard Typography page.
