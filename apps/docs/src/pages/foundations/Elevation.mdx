import { SparcShadowGrid } from '../../components/foundations/SparcFoundation';

<PageHeader
  title="Elevation"
  description="Shadow tokens collected from the SPARC apps (carltonLaborPlanning globals.css, verified identical to commonforge-sparc-ui). Collected 2026-07-21."
/>

<div className="my-6 rounded-lg border border-default bg-muted/40 p-4 text-sm text-default">
  <p className="font-semibold text-subtle">Provenance</p>
  <p className="mt-1 text-subtle">
    These tokens were collected from the SPARC apps (carltonLaborPlanning +
    commonforge-sparc-ui) on 2026-07-21 and are this repo's token source. Values
    are rendered straight from <code>src/data/sparc-foundation.json</code> via
    inline styles.
  </p>
</div>

## Token shadows

Three token shadows: `elev-1` for cards, `elev-2` for raised cards, `pop` for
modals. All use the warm charcoal `rgba(32,27,24,…)` base.

<SparcShadowGrid />

| Token | Use |
|---|---|
| `shadow-elev-1` | cards |
| `shadow-elev-2` | raised cards |
| `shadow-pop` | modals |

## Shared shadow recipes (not yet tokens)

Four multi-layer shadow recipes live in component code, identical in both SPARC
apps — candidates to promote into tokens:

| Recipe | Used by |
|---|---|
| Button shadow (6 layers incl. inset highlights) | button, segmented pill, date-picker Apply |
| Card shadow "003" (5 layers) | kpi-card, dashboard panels, job cards |
| Popover/menu shadow (3 layers) | dropdowns, menus, date-picker card |
| Chart tooltip shadow (6 layers) | chart-tooltip |

## Source of truth

Values on this page come from `apps/docs/src/data/sparc-foundation.json`, copied
unchanged from the original collection (`design-system-testing-abdillah/foundation/tokens.json`,
v0.2.0); recipes documented from `foundation.md` §3. This collection is this
repo's token source.
