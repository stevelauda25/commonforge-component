{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "corepack enable && pnpm install --frozen-lockfile -C .. && pnpm -C .. build:packages && npm install --no-fund --no-audit",
  "buildCommand": "npm run build",
  "framework": "nextjs"
}
