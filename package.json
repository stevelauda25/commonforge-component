{
  "name": "pod-design-system-test",
  "private": true,
  "version": "0.1.12",
  "description": "Small design system test environment for POD — sprint-style scope.",
  "packageManager": "pnpm@9.7.0",
  "scripts": {
    "dev": "pnpm -F docs dev",
    "build": "pnpm build:docs",
    "build:packages": "pnpm -F @commonforge/tokens build && pnpm -F @commonforge/ui build",
    "build:docs": "pnpm build:packages && mkdir -p apps/docs/public && echo '{\"drifted\":[],\"inSync\":[],\"errors\":[]}' > apps/docs/public/figma-status.json && echo '{\"components\":[]}' > apps/docs/public/figma-manifest.json && pnpm -F docs build",
    "typecheck": "pnpm -r --parallel typecheck",
    "clean": "rm -rf node_modules **/node_modules **/dist"
  },
  "devDependencies": {
    "tsup": "^8.5.1",
    "typescript": "^5.5.3"
  },
  "engines": {
    "node": ">=18.17"
  }
}
